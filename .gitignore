node_modules/
# Elastic Beanstalk Files
.elasticbeanstalk/*
!.elasticbeanstalk/*.cfg.yml
!.elasticbeanstalk/*.global.yml
node_modules/
uploads/**/*.pdf
test/**/*.pdf
.vercel
.env*.local

# Dev-user data dumps (from scripts/diag-* + delete jobs — never commit)
all-dev-users-*.csv
all-dev-users-*.json
deleted-dev-users-*.csv
deleted-dev-users-*.json

# Local .env backups (e.g. after sed -i '.bak')
.env.bak
